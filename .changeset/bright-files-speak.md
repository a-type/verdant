---
'@verdant-web/common': patch
'@verdant-web/store': minor
---

Add alt text built-in to files. All files have an optional `alt` property which is synchronized with them. Now it's easy to support alt text for images!
